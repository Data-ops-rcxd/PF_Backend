import {  } from "./users.controller.js";
import {Router} from 'express';
const router = Router();

// Endpoint GET
router.get('/:number',  );
router.get('/:email/:cdi',  );

// Endpoint POST
router.post('/',  );

// Endpoint PATCH
router.patch('/:id',  );

// Endpoint DELETE
router.delete('/:id',  );

export default router;